// To be run when the head workspace is the active project
() {
    ~ string GENERIC_FP = "./generic.ses";

    ~ string[] IDs = [
        "standard-head"
    ];

    ~ script generic = $SE.read_script(GENERIC_FP);
    generic.run(IDs, "head", $SE.get_project().get_layer("head"));
}
